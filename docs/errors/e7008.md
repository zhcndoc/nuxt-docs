---
title: 'NUXT_E7008'
description: 'callOnce fn 必须是一个函数。'
navigation: false
---

# E7008

`callOnce()` 收到了一个非函数的 `fn` 参数。要只执行一次的值必须是一个函数，这样 Nuxt 才能将其执行一次，并在后续调用中跳过它。

## 分辨率

```ts
await callOnce('setup', () => {
  // 仅运行一次
})
```

::read-more{to="/docs/api/utils/call-once"}
::
