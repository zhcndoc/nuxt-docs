---
title: 'NUXT_E7010'
description: 'callOnce 键必须是字符串。'
navigation: false
---

# E7010

`callOnce()` 被调用时传入了一个不是字符串的键。该键用于跟踪函数是否已经运行，因此它必须是一个非空字符串。

## 分辨率

```ts
await callOnce('setup', () => {
  // 仅运行一次
})
```

::read-more{to="/docs/api/utils/call-once"}
::
