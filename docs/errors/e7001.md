---
title: 'NUXT_E7001'
description: '载荷 URL 包含主机名。'
navigation: false
---

# E7001

请求的载荷 URL 使用了包含主机名的完整 URL。载荷始终从与应用相同的源提供，因此包含主机名的 URL 会指向应用外部并被拒绝。

## 解决方案

传递相对路径，而不是完整 URL：

```ts
loadPayload('/some-page')
```

::read-more{to="/docs/api/composables/use-nuxt-app#payload"}
::
