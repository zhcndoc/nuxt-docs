---
title: ".nuxtrc"
description: ".nuxtrc 文件允许你以扁平语法定义 Nuxt 配置。"
head.title: ".nuxtrc"
navigation.icon: i-vscode-icons-file-type-nuxt
---

`.nuxtrc` 文件可用于以扁平语法配置 Nuxt。它基于 [`unjs/rc9`](https://github.com/unjs/rc9)。

::tip
有关更高级的配置，请使用 [`nuxt.config`](/docs/4.x/guide/directory-structure/nuxt-config)。
::

## 用法

```bash [.nuxtrc]
# 禁用 SSR
ssr=false

# `@nuxt/devtools` 的配置
devtools.enabled=true

# 添加 Nuxt 模块
modules[]=@nuxt/image
modules[]=nuxt-security
```

如果存在，`nuxt.config` 文件中的属性将覆盖 `.nuxtrc` 文件中的属性。

::note
Nuxt 会自动添加一个 `setups` 部分来跟踪模块的安装和升级状态。此部分在内部用于 [模块生命周期钩子](/docs/4.x/api/kit/modules#using-lifecycle-hooks-for-module-installation-and-upgrade)，不应手动修改。
::

::read-more{to="/docs/4.x/api/configuration/nuxt-config"}
在 **Nuxt 配置** 文档中查看所有可用选项。
::

## 全局 `.nuxtrc` 文件

你也可以在主目录中创建一个全局 `.nuxtrc` 文件，以在全局应用配置。

- 在 macOS/Linux 上，此文件位于：

  ```md
  ~/.nuxtrc
  ```

- 在 Windows 上，位于：

  ```md
  C:\Users\{username}\.nuxtrc
  ```

此全局 `.nuxtrc` 文件允许你为系统上的所有 Nuxt 项目定义默认设置。然而，项目级别的 `.nuxtrc` 文件会覆盖这些全局设置，且 `nuxt.config` 将优先于两者。