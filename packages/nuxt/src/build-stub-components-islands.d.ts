/**
 * Named-export stub for `#build/components.islands.mjs`, used only via `paths`
 * in `tsconfig.build.json` while emitting this package's declarations (the
 * catch-all `build-stub-module.d.ts` uses `export =`, which cannot satisfy the
 * named ESM imports in `island-renderer.ts` under `verbatimModuleSyntax`). The
 * names mirror `componentsIslandsTemplate` in `./core/templates.ts`.
 */
declare const anyValue: any

export {
  anyValue as islandComponents,
  anyValue as pageIslandRoutes,
}
